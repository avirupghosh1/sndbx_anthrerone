{{- if .Values.ingress.enabled }}
kind: Ingress
apiVersion: {{ .Values.api.ingress | quote }}
metadata:
  name: {{ include "agent-sandbox.fullname" . }}-ingress
  namespace: {{ .Values.namespace }}
  labels:
{{ include "agent-sandbox.labels" . | indent 4 }}
  annotations:
{{ toYaml .Values.ingress.annotations | indent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className | quote }}
  rules:
  {{- range .Values.ingress.apiHosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "agent-sandbox.apiServiceName" $ }}
                port:
                  number: {{ $.Values.service.apiService.port }}
  {{- end }}
  {{- range .Values.ingress.sandboxHosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "agent-sandbox.runtimeGatewayName" $ }}
                port:
                  number: {{ $.Values.service.runtimeGateway.httpPort }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - secretName: {{ .Values.ingress.tls.secretName | quote }}
      hosts:
{{ toYaml .Values.ingress.tls.hosts | indent 8 }}
  {{- end }}
{{- end }}
