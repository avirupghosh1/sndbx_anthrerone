fastapi==0.115.6
uvicorn==0.34.0
# 2.5.x pydantic-core fails to build on Python 3.12.4+ / 3.13 / 3.14 (ForwardRef._evaluate / recursive_guard).
# Use 2.12+ for current CPython typing and prebuilt wheels.
pydantic>=2.12.0,<3
# 7.1+ required for requests 2.32+ (fixes "Not supported URL scheme http+docker")
# [ssh] pulls Paramiko so DOCKER_HOST=ssh://… (remote Linux VM) works with docker-py
docker[ssh]>=7.1.0,<8
httpx==0.28.1
websockets>=12.0,<15
python-dotenv==1.0.0
# Dockerfile → in-container RUN/COPY/… (E2B-style parsed build); see orchestrator/template_dockerfile_builder.py
dockerfile-parse>=2.0.0,<3
kubernetes>=28.1.0,<35
